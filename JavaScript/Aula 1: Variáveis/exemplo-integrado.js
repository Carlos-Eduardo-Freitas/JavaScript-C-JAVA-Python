// Exemplo integrado da aula: Variáveis, Tipos e Operadores

// Entrada
const idade = 19;
const nota = 7.2;
const frequencia = 82;

// Processamento
const aprovado = (idade >= 18) && (nota >= 6.0);
const situacaoRegular = aprovado && (frequencia >= 75);

// Saída
console.log("Aprovado:", aprovado);           // true
console.log("Situação regular:", situacaoRegular); // true
